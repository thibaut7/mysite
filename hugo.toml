baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'My New Hugo Site'
theme = 'archie'

[build]
  command = "hugo"
  publish = "public"

[context.production.environment]
  HUGO_VERSION = "latest" # Or specify a specific version, e.g., "0.118.2"
